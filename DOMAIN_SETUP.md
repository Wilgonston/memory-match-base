# 🌐 Настройка Домена

## Быстрая Инструкция

### 1. Замени Placeholders в `index.html`

Открой `index.html` и замени все `REPLACE_WITH_YOUR_DOMAIN` на свой домен:

```bash
# Найди и замени (Ctrl+H в VS Code):
REPLACE_WITH_YOUR_DOMAIN → your-actual-domain.com
REPLACE_WITH_YOUR_TWITTER → your_twitter_handle
```

**Пример:**
```html
<!-- Было: -->
<meta property="og:url" content="https://REPLACE_WITH_YOUR_DOMAIN/" />

<!-- Стало: -->
<meta property="og:url" content="https://memory-match.vercel.app/" />
```

### 2. Настрой Custom Domain в Vercel

#### Вариант A: Vercel Domain (бесплатно)
Твой проект уже доступен по адресу:
```
https://your-project-name.vercel.app
```

#### Вариант B: Custom Domain (если купил домен)

1. **Открой Vercel Dashboard**
   - Перейди в свой проект
   - Settings → Domains

2. **Добавь домен**
   - Введи свой домен (например: `memory-match.com`)
   - Нажми "Add"

3. **Настрой DNS у регистратора домена**

   Vercel покажет DNS записи, которые нужно добавить:

   **Для корневого домена (memory-match.com):**
   ```
   Type: A
   Name: @
   Value: 76.76.21.21
   ```

   **Для www (www.memory-match.com):**
   ```
   Type: CNAME
   Name: www
   Value: cname.vercel-dns.com
   ```

4. **Подожди 5-60 минут**
   - DNS обновление занимает время
   - Vercel автоматически выдаст SSL сертификат

### 3. Обнови index.html с реальным доменом

После настройки домена:

```bash
# 1. Обнови index.html с реальным доменом
# 2. Закоммить изменения
git add index.html
git commit -m "Update domain in meta tags"
git push

# Vercel автоматически задеплоит обновления
```

### 4. Проверь

Открой свой домен и проверь:
- ✅ Сайт загружается
- ✅ SSL работает (https://)
- ✅ Wallet подключается
- ✅ Игра работает

## Популярные Регистраторы Доменов

- **Namecheap** - https://www.namecheap.com/
- **GoDaddy** - https://www.godaddy.com/
- **Cloudflare** - https://www.cloudflare.com/products/registrar/
- **Google Domains** - https://domains.google/

## Troubleshooting

### Домен не работает
- Проверь DNS записи (может занять до 48 часов)
- Используй https://dnschecker.org/ для проверки

### SSL не работает
- Vercel автоматически выдаст SSL через Let's Encrypt
- Подожди 5-10 минут после настройки DNS

### Нужна помощь?
Спроси меня! 😊
