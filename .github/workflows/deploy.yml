name: Deploy to GitHub Pages

on:
  push:
    branches: [ main ]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout
      uses: actions/checkout@v4
    
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '18'
        cache: 'npm'
    
    - name: Install dependencies
      run: npm ci
    
    - name: Run tests
      run: npm test -- --run
    
    - name: Build
      run: npm run build
      env:
        VITE_NETWORK: ${{ secrets.VITE_NETWORK }}
        VITE_CONTRACT_ADDRESS: ${{ secrets.VITE_CONTRACT_ADDRESS }}
        VITE_ONCHAINKIT_API_KEY: ${{ secrets.VITE_ONCHAINKIT_API_KEY }}
        VITE_CDP_PROJECT_ID: ${{ secrets.VITE_CDP_PROJECT_ID }}
        VITE_WALLETCONNECT_PROJECT_ID: ${{ secrets.VITE_WALLETCONNECT_PROJECT_ID }}
        VITE_APP_URL: ${{ secrets.VITE_APP_URL }}
        VITE_FARCASTER_FID: ${{ secrets.VITE_FARCASTER_FID }}
        VITE_FARCASTER_CUSTODY_ADDRESS: ${{ secrets.VITE_FARCASTER_CUSTODY_ADDRESS }}
        VITE_ACCOUNT_ASSOCIATION_SIGNATURE: ${{ secrets.VITE_ACCOUNT_ASSOCIATION_SIGNATURE }}
        VITE_ACCOUNT_ASSOCIATION_TIMESTAMP: ${{ secrets.VITE_ACCOUNT_ASSOCIATION_TIMESTAMP }}
    
    - name: Setup Pages
      uses: actions/configure-pages@v4
    
    - name: Upload artifact
      uses: actions/upload-pages-artifact@v3
      with:
        path: './dist'
    
    - name: Deploy to GitHub Pages
      id: deployment
      uses: actions/deploy-pages@v4
